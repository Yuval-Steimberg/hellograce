import MobileHero from "./MobileHero";
import DesktopHero from "./DesktopHero";

/** Hero — chat-led on mobile, an interactive capability cluster on desktop. */
const HeroSection = () => (
  <>
    <MobileHero />
    <DesktopHero />
  </>
);

export default HeroSection;
