/**
 * No decorative background — the site uses a plain white background per design
 * direction. Kept as a no-op component so existing imports stay valid.
 */
const AnimatedBackground = () => null;

export default AnimatedBackground;
