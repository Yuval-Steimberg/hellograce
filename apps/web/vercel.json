{
  "buildCommand": "pnpm --filter @grace/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install -g pnpm@10.4.1 && pnpm install --filter @grace/web... --no-frozen-lockfile --ignore-scripts",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
