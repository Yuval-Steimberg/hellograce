{
  "buildCommand": "pnpm --filter @grace/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install --filter @grace/web... --no-frozen-lockfile --ignore-scripts",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
